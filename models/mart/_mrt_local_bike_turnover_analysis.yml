version: 2

models:
  - name: mrt_local_bike_turnover_analysis
    description: '{{ doc("mrt_local_bike_turnover_analysis") }}'
    columns:
      - name: store_id
        description: "Primary key, unique identifier for each store."
        tests:
          - unique
          - not_null